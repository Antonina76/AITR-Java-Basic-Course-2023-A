import java.util.Scanner;

//public class Stern {
  //  public static void main(String[] args) {

        // Написать программу, которая принимает с клавиатуры номер месяца и год и
        // выводит на экран
        // название месяца и количество дней в нем. (повторение условного оператора)

    /*    System.out.println("Seasons");
        Scanner scanner = new Scanner(System.in);
        System.out.println("Please input number of month  and year: ");
        String monthYear = scanner.nextLine();
        int l = monthYear.length();
        int index = monthYear.indexOf(" ");
        String month = monthYear.substring(0, index);
        month = month.trim();
        int month1 = Integer.parseInt(month);
        System.out.println(month1);
        String year = monthYear.substring(index, l);
        year = year.trim();
        int year1 = Integer.parseInt(year);
        System.out.println(year);
        if (month1 == 1) {
            System.out.println("The month" + month1 + "is January." + " It has 31 days. ");
        }
        if (month1 == 2) {
            System.out.println("The month " + month1 + "is February. " + "It has 28 days. ");
           // int year2 = leapYear (year1);
        } else {
           // System.out.println("The month " + month1 + "is February. " + "It has 29 days. "););
        }

        //public static boolean leapYear(int year2) {
       //     if (year2 % 4 == 0)
         //       leapYear = true;
         //   if (year2 % 100 == 0)
          //      leapYear = false;
         //   if (year2 % 400 == 0)
          //      leapYear = true;

        }
        }

    }


*/

